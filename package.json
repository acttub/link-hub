{
  "name": "link-hub",
  "version": "0.0.0",
  "private": true,
  "description": "link.acttub.com — 인스타 바이오 링크 허브. 스타일만 Tailwind CLI로 굽고, 배포는 정적이다.",
  "scripts": {
    "css": "tailwindcss -i src/input.css -o styles.css --minify",
    "css:watch": "tailwindcss -i src/input.css -o styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
